export interface RecipeInputDTO {
    title: string,
    description: string,
    authorId: string
}

export interface InsertRecipeDTO {
    id: string,
    title: string,
    description: string,
    authorId: string
}